
<div id="send-resume-vacancy">
    <div>
        <h3 style="margin-top: 5px">Виберіть резюме</h3>
    </div>
    <div>
        <?php echo Form::open(['route' => ['company.response.sendResume',$company->id],'method'=>"POST"]); ?>

        <div class="form-group <?php echo e($errors-> has('Load') ? 'has-error' : ''); ?>" >
            <?php if(!empty($resume)): ?>
                <select class="form-control" id="resume" name="resumeId" style="margin-top: 10px">
                    <?php foreach($resume as $res): ?>
                        <option value="<?php echo e($res->id); ?>" selected><?php echo e($res->position); ?></option>
                    <?php endforeach; ?>
                </select>
        </div>

        <?php echo Form::hidden('id', $company->id, array('class' => 'form-control')); ?>

        <?php else: ?>
            <p>У вас немає резюме.Перейти до створення резюме</p>
            <p><?php echo link_to_route('resume.create','Створення резюме','','style="color:#f68c06"'); ?></p>
        <?php endif; ?>
        <div>
            <?php if(!empty($resume)): ?>
                <div align="right">
                    <?php echo Form::submit('Відправити', ['class' => 'btn btn-warning btn-send']); ?>

                </div>
            <?php endif; ?>
        </div>
    </div>


    <?php echo Form::close(); ?>

</div>